language: python
python:
  - 2.7
  - 3.4
  - 3.5
  - 3.6
sudo: false

install:
  - pip install -U packaging  # fix for Python3.4 build
  - pip install tox tox-travis coveralls
script:
  - tox
after_success:
  coveralls
